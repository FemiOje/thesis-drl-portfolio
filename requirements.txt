# Pinned to the versions declared in the thesis, Chapter 3 §3.3.
#
# REQUIRES PYTHON 3.10. numpy 1.24.3, pandas 2.0.3 and torch 2.1.0 publish no
# cp312 wheels, so `pip install -r requirements.txt` fails outright on Python 3.12.

numpy==1.24.3
pandas==2.0.3
torch==2.1.0
gymnasium==0.29.1
stable-baselines3==2.2.1
yfinance==0.2.54   # last requests-only release; curl_cffi (>=0.2.55) blocked by Application Control
matplotlib==3.7.2
seaborn==0.12.2
scipy==1.11.4

# Tooling (not thesis-declared)
PyYAML==6.0.1
pytest==7.4.4
