# Environment that produced the reported results (Windows, Python 3.12).
#
# numpy==1.26.4 is a RECORD, not a constraint. stable-baselines3 2.9.0 declares
# numpy<3.0,>=1.20 and nothing in this codebase uses a numpy-2-removed alias, so
# numpy 2.x works fine. Do NOT apply this pin on Colab: its pandas and torch are
# compiled against numpy 2.x and downgrading breaks them with
#   ValueError: numpy.dtype size changed, may indicate binary incompatibility
# See notebooks/colab_results.ipynb, which installs only what Colab lacks.
torch==2.13.0
stable-baselines3==2.9.0
gymnasium==1.0.0
numpy==1.26.4
pandas==2.3.3
yfinance==1.5.2
matplotlib==3.11.1
tensorboard==2.21.0
pytest==9.1.1
